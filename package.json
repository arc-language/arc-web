{
  "name": "arc",
  "version": "0.1.0",
  "description": "Arc — a new language for the web. Compiles to optimal HTML, CSS, and JS.",
  "main": "src/cli.js",
  "bin": {
    "arc": "src/cli.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/arc-lang/arc"
  },
  "files": ["src", "stdlib", "adp"],
  "scripts": {
    "test": "node --test 'tests/**/*.test.js'",
    "test:coverage": "node --test --experimental-test-coverage 'tests/**/*.test.js'",
    "build": "node src/cli.js build",
    "dev": "node src/cli.js dev"
  },
  "keywords": ["arc", "compiler", "web", "language"],
  "license": "MIT",
  "devDependencies": {}
}
